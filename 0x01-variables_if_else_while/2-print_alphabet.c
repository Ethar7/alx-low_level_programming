#include<stdio.h>
/**
 * main - Entry point
 *
 * Description: function that prints alphapit
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
char n = 'a';
	while (n >= 'a')
	{
	putchar(n);
	a++;
	}
	putchar(\n);
	return (0);
}
